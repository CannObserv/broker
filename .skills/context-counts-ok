# Counts in the policy file that check-counts.sh reported and a person judged.
# Grammar and the closed warrant set: check-counts.sh --help.

# 2026-09-22 curation.

# The three callers are listed in the same sentence - the probe, an operator at
# a redis-cli, and the deploy tests - so the number is checkable by reading it.
AGENTS.md :: enumerated :: That credential is three callers

# tests/deploy/test_redis_acl.py asserts archiver's +xtrim selector, so the
# count of DLQs in it cannot drift without that suite going red.
AGENTS.md :: gated :: its two DLQs, live 2026-09-22

# tests/test_bus_health.py::test_one_missed_republish_is_absorbed_and_two_are_not
# pins both sides of the boundary, parametrized over four set sizes; the "three"
# is LWW_WARN_LAST_ENTRY_AGE_SECONDS == 3 * LWW_REPUBLISH_PERIOD_SECONDS, which
# the same test asserts.
AGENTS.md :: gated :: absorbs one missed republish and not two
