# Counts in the policy file that check-counts.sh reported and a person judged.
# Grammar and the closed warrant set: check-counts.sh --help.

# 2026-09-22 curation.

# The three callers are listed in the same sentence - the probe, an operator at
# a redis-cli, and the deploy tests - so the number is checkable by reading it.
AGENTS.md :: enumerated :: That credential is three callers

# Two `gated` entries pruned 2026-09-24: "its two DLQs, live 2026-09-22" left
# AGENTS.md for README.md *Provenance* in that day's curation, and "absorbs one
# missed republish and not two" went with broker#45, which closed the gap it
# described.

# 2026-09-24 curation: the five are listed on the next line of the same bullet;
# "the other four" is those five less content.blobs, two lines below.
AGENTS.md :: enumerated :: No retention opinion on the five `content.*` streams
AGENTS.md :: enumerated :: the other four joined it
