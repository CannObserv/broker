# Cross-references check-seams.sh reported and a person judged legitimate.
# A hit is wrong only if what it points at moved; each entry below points at
# something that did not. Grammar: check-seams.sh --help.

# --- the policy file (AGENTS.md has not moved) --------------------------------
docs/SKILLS.md :: (`AGENTS.md` and the docs it links)
docs/SKILLS.md :: | `curating-context` | curate context, context budget, trim AGENTS.md |

# --- 2026-09-11 split of docs/STREAMS.md into STREAMS.md + BUS-HEALTH.md ------
# BUS-HEALTH.md pointing back at what stayed in STREAMS.md: the table, its
# columns, and *Who drains a DLQ*.
docs/BUS-HEALTH.md :: and who drains its DLQ is [STREAMS.md](STREAMS.md)
docs/BUS-HEALTH.md :: Moved out of STREAMS.md on 2026-09-11
docs/BUS-HEALTH.md :: The `info.changes` row's `Health primitive` cell in [STREAMS.md](STREAMS.md)
docs/BUS-HEALTH.md :: Note the *permanently* in `content.fetch-policy`'s STREAMS.md row.
docs/BUS-HEALTH.md :: see *Who drains a DLQ* in [STREAMS.md](STREAMS.md);
docs/BUS-HEALTH.md :: retention trims in the [STREAMS.md](STREAMS.md) table.
docs/BUS-HEALTH.md :: `Producer durability under OOM` column of [STREAMS.md](STREAMS.md).
docs/BUS-HEALTH.md :: *Who drains a DLQ* in [STREAMS.md](STREAMS.md) - an assignment
# Mentions of the probe inside the doc the probe's section now lives in.
docs/BUS-HEALTH.md :: What the bus-health probe watches, the per-stream contracts
docs/BUS-HEALTH.md :: (badge + journald line) and the bus-health probe
# Non-stream keys stayed in STREAMS.md.
docs/RECOVERY.md :: [STREAMS.md](STREAMS.md), *Non-stream keys on `db0`*.
# Source pointing at STREAMS.md content that stayed: the inventory rows, the
# content.replicate no-trim carve-out, non-stream keys, Who drains a DLQ.
deploy/redis-acl.conf :: hazard ../docs/STREAMS.md carves that stream out of the trim set for.
deploy/redis-acl.conf :: inventoried in ../docs/STREAMS.md,
deploy/redis.conf.broker :: # ../docs/STREAMS.md grows.
src/broker/bus_health.py :: ``docs/STREAMS.md`` orders the drain
src/broker/bus_health.py :: # docs/STREAMS.md, "Who drains a DLQ", and mirrored here
src/broker/bus_health.py :: mirroring the ``docs/STREAMS.md`` inventory.
src/broker/bus_health.py :: resting state is 0; see docs/STREAMS.md, "Who drains a DLQ"
src/broker/bus_health.py :: these payloads are gone. See docs/STREAMS.md, "Who drains a DLQ"
tests/deploy/test_live_broker_matches_tracked_config.py :: eviction candidate set; see docs/STREAMS.md,
tests/deploy/test_redis_acl.py :: the inventory row is in ../docs/STREAMS.md.
tests/deploy/test_redis_acl.py :: which is the exact hazard `docs/STREAMS.md` carves
# The group-missing finding (CR 1) pointing at the probe's section where it now
# lives; tests/test_bus_health.py resolves it against the heading every run.
src/broker/bus_health.py :: stream was recreated; see docs/BUS-HEALTH.md, "The bus-health probe"
# The probe named as a thing, not as a pointer into a doc.
deploy/redis-acl.conf :: # The bus-health probe and the backstop DLQ drainer.
pyproject.toml :: Operational code for the Cannabis Observer change-bus broker

# --- 2026-09-11 split of docs/RESTART-WINDOW.md into + ACL-CUTOVER.md ---------
# ACL-CUTOVER.md pointing back at what stayed: the window, the identities
# block at the top, step 1d.
docs/ACL-CUTOVER.md :: ([RESTART-WINDOW.md](RESTART-WINDOW.md)): before it - minting the passwords
docs/ACL-CUTOVER.md :: stay in RESTART-WINDOW.md.
docs/ACL-CUTOVER.md :: RESTART-WINDOW.md; `$U` (`default:`) authenticates only
docs/ACL-CUTOVER.md :: continue from Step 1 (1a to 1d) in RESTART-WINDOW.md
docs/ACL-CUTOVER.md :: Moved out of RESTART-WINDOW.md on 2026-09-11
docs/ACL-CUTOVER.md :: and after the restart in [RESTART-WINDOW.md](RESTART-WINDOW.md) step 1d.
# The `nopass` trap named where it now lives, or linked to it.
docs/ACL-CUTOVER.md :: plus the `nopass` trap the dry run exists to catch
docs/RESTART-WINDOW.md :: `nopass` trap - are [ACL-CUTOVER.md](ACL-CUTOVER.md).
docs/RESTART-WINDOW.md :: the `nopass` trap ([ACL-CUTOVER.md](ACL-CUTOVER.md))
# The incident and step 1a-bis / step 1d stayed in RESTART-WINDOW.md.
docs/RECOVERY.md :: the incident in `docs/RESTART-WINDOW.md` was
docs/SKILLS.md :: [RESTART-WINDOW.md](RESTART-WINDOW.md), never a ship step.
deploy/redis.conf.broker :: docs/RESTART-WINDOW.md step 1a-bis is the runbook
tests/deploy/test_live_acl_matches_tracked_acl.py :: ``docs/RESTART-WINDOW.md`` step 1d runs
tests/deploy/test_live_acl_matches_tracked_acl.py :: docs/RESTART-WINDOW.md step 1d runs this suite
tests/deploy/test_redis_acl.py :: That sequence is recorded in docs/RESTART-WINDOW.md

# 2026-09-18 curation. The Code Exploration Policy block did NOT move in this
# run - it is still the `## Code Exploration Policy` section of AGENTS.md, where
# init-socraticode puts it - so a detail doc pointing back at it is describing
# the surface correctly, not naming a stale location.
docs/SOCRATICODE.md :: Detail doc for the `## Code Exploration Policy` block in `AGENTS.md`
docs/SOCRATICODE.md :: heading down there, not in `AGENTS.md`
docs/SOCRATICODE.md :: `AGENTS.md` block should be on its degraded variant
#
# The 2026-09-18 split seams, judged. STREAMS.md kept its stream inventory, its
# participants TABLE and *Who drains a DLQ*; only the latency/DERP measurements
# and the one-time reap left it. RESTART-WINDOW.md kept every step; only the
# incident record left. So each reference below still points at content that is
# where it says it is. The one that did not - RECOVERY.md's pointer at *Risk:
# the tailnet relay* - was repointed at NETWORK-PATHS.md rather than acked.
docs/ACL-CUTOVER.md :: in [STREAMS.md](STREAMS.md), *Participants, hosts and paths*
docs/SOCRATICODE.md :: - `docs/STREAMS.md` claims who produces, consumes and drains each stream
.socraticodecontextartifacts.json :: "path": "./docs/STREAMS.md"
.socraticodecontextartifacts.json :: "path": "./docs/RESTART-WINDOW.md"
deploy/redis-acl.conf :: # publish selector naming only what ../docs/STREAMS.md says it produces
deploy/redis-acl.conf :: # issue a command onto it and cannot cap it. That is ../docs/STREAMS.md's
deploy/redis-acl.conf :: # ../docs/STREAMS.md documents for a queue nobody claimed
tests/deploy/test_redis_acl.py :: """``stream -> the service that produces it``, read off ../docs/STREAMS.md.
tests/deploy/test_redis_acl.py :: The sharpest case is `content.replicate`, whose row in ../docs/STREAMS.md
tests/deploy/test_redis_acl.py :: delivered and orphans the PEL entries naming them, so ../docs/STREAMS.md
tests/deploy/test_redis_acl.py :: them. ../docs/STREAMS.md says it is never trimmed, and archiver's drain loop
tests/deploy/test_memory_protection.py :: all of it under copy-on-write, the worst case ``docs/RESTART-WINDOW.md``
docs/RECOVERY.md :: have no established bound (`docs/RESTART-WINDOW.md`, *Step 1d*)
# The new docs' own provenance lines - a demotion naming where it came from.
docs/CONSUMER-REGISTRATIONS.md :: [STREAMS.md](STREAMS.md); what the probe watches on them is
docs/CONSUMER-REGISTRATIONS.md :: [BUS-HEALTH.md](BUS-HEALTH.md). Moved out of STREAMS.md when it ran past the
docs/NETWORK-PATHS.md :: [STREAMS.md](STREAMS.md), *Participants, hosts and paths* - this is the
docs/NETWORK-PATHS.md :: measurement half of that section, moved out when STREAMS.md ran past the
docs/NETWORK-PATHS.md :: exact position with nothing pending ([RESTART-WINDOW.md](RESTART-WINDOW.md))
docs/INCIDENT-2026-09-10.md :: [RESTART-WINDOW.md](RESTART-WINDOW.md). Moved out of it when that file ran

# --- 2026-09-22 split of docs/BUS-HEALTH.md into + MEMORY-PROTECTION.md and
# --- UNDELIVERED-CONSUMERS.md -------------------------------------------------
# Only two topics left the doc: producer behaviour under the `maxmemory` cap
# (with the `noeviction` argument), and the `group-undelivered` check's
# reasoning. Everything every line below points at - the probe and its per-tick
# checks, *Mirrored constants*, *The one cap that is read, not mirrored*,
# *Detecting loss*, *Who watches what* - stayed. The three that did NOT still
# point at what they name were retargeted rather than acked: AGENTS.md's
# `noeviction` rule, STREAMS.md's monitoring sentence, and the two runtime
# finding messages in src/broker/bus_health.py.
#
# The two new docs pointing back at the probe that runs what they describe.
docs/MEMORY-PROTECTION.md :: `eviction-policy` finding - is [BUS-HEALTH.md](BUS-HEALTH.md).
docs/UNDELIVERED-CONSUMERS.md :: [BUS-HEALTH.md](BUS-HEALTH.md); which stream carries which group is
# Their provenance lines - a demotion naming where it came from.
docs/MEMORY-PROTECTION.md :: Moved out of BUS-HEALTH.md on 2026-09-22, when it ran past the per-doc
docs/UNDELIVERED-CONSUMERS.md :: Moved out of BUS-HEALTH.md on 2026-09-22, when it ran past the per-doc
# STREAMS.md: the monitoring sentence after its noeviction clause was repointed,
# and the two LWW rows naming *The one cap that is read, not mirrored*.
docs/STREAMS.md :: what* - is [BUS-HEALTH.md](BUS-HEALTH.md). The `noeviction` contract is
docs/STREAMS.md :: | `content.fetch-policy` | Watcher → Replicator workers
docs/STREAMS.md :: | `info.watch-status` | Watcher → **Archiver**
# The index entry for the doc itself, which did not move.
.socraticodecontextartifacts.json :: "path": "./docs/BUS-HEALTH.md",
# Source and tests naming sections that stayed. The last is pinned every run by
# test_the_doc_section_a_finding_points_at_exists.
src/broker/bus_health.py :: Outbox monitoring is therefore archiver's; see `docs/BUS-HEALTH.md` for the
src/broker/bus_health.py :: # See docs/BUS-HEALTH.md, "Mirrored constants".
src/broker/bus_health.py :: and docs/BUS-HEALTH.md for the measured tables.
src/broker/bus_health.py :: tail = 'See docs/BUS-HEALTH.md, "The bus-health probe"'
tests/test_bus_health.py :: set sizes docs/BUS-HEALTH.md says it was measured at, so the claim there
tests/test_bus_health.py :: named on each (docs/BUS-HEALTH.md, "Mirrored constants")."""
tests/test_bus_health.py :: three causes (docs/BUS-HEALTH.md), and a message asserting it would send
