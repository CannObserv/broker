# Lines a curation had to rewrite rather than move - one judged entry per line.
# Grammar and the closed warrant set: prove-no-loss.sh --help.
# Entries are PER-TARGET: prove-no-loss.sh consults only those matching the
# --file it was run against, so a run reporting some of these as
# "matched nothing" is usually sweeping a different target, not finding
# them stale. Prune only what you have re-judged against its own target.

# 2026-09-11 curation (STREAMS.md -> + BUS-HEALTH.md, RESTART-WINDOW.md -> + ACL-CUTOVER.md)

# AGENTS.md - counts dropped (check-counts), same claims
AGENTS.md :: tighten :: - **The `OOM` seam spans two repos.**
AGENTS.md :: tighten :: keyspace.
AGENTS.md :: tighten :: - **Mirrored constants.** The three retention caps in
AGENTS.md :: tighten :: Two things commit to `main` unprompted:
# AGENTS.md - pointers into sections this change moved to docs/BUS-HEALTH.md
AGENTS.md :: retarget :: that section; two tests pin it, one on the config and one on the live
AGENTS.md :: retarget :: all. See `docs/STREAMS.md`, "Mirrored constants".
# AGENTS.md - refuted by `gh issue view`: archiver#196 closed COMPLETED 2026-09-09,
# and archiver's publisher.py now cites deploy/redis.conf.broker
AGENTS.md :: disproven :: in Phase 5, so **archiver's half still
AGENTS.md :: disproven :: points at the old path** until it is updated - CannObserv/archiver#196.
AGENTS.md :: disproven :: archiver's half of the OOM seam, stale since the
# AGENTS.md - refuted by `gh issue view`: broker#2..#7 closed COMPLETED, only #8 open
AGENTS.md :: disproven :: Phases 1-4 done; remaining work is
AGENTS.md :: disproven :: its sub-issues: #2 ACL users, #3 alerting, #4 backup
AGENTS.md :: disproven :: #6 OOM contract, #7 exercise two streams, #8 latency matrix.

# docs/STREAMS.md - provenance moved out of headings (check-seams provenance-heading)
docs/STREAMS.md :: rename :: ## Non-stream keys on `db0` (CannObserv/broker#9, CannObserv/replicator#80)
docs/STREAMS.md :: rename :: ## Who drains a DLQ (CannObserv/archiver#162)
docs/STREAMS.md :: rename :: ## Orphaned consumer registrations, one time only (CannObserv/archiver#156)
# docs/STREAMS.md - "above" / "in this file" pointers now crossing into BUS-HEALTH.md
docs/STREAMS.md :: retarget :: see *Who drains a DLQ* above;
docs/STREAMS.md :: retarget :: retention trims in the table above.
docs/STREAMS.md :: retarget :: `Producer durability under OOM` column above.
docs/STREAMS.md :: retarget :: *Who drains a DLQ* above, in this file

# docs/RESTART-WINDOW.md - pointers into sections this change moved to ACL-CUTOVER.md
docs/RESTART-WINDOW.md :: retarget :: close it by disabling it again - step 4, both directions.
docs/RESTART-WINDOW.md :: retarget :: The dry run below has been done
docs/RESTART-WINDOW.md :: retarget :: | the `nopass` trap | roll back step 1 immediately
# moved to ACL-CUTOVER.md with the trap; one of its two "above" instances is now step 1d over there
docs/RESTART-WINDOW.md :: retarget :: It appears twice above, before and after the restart
# fresh-eyes review: step 4 and the dry run now live in ACL-CUTOVER.md
docs/RESTART-WINDOW.md :: retarget :: step 4's rollback - and step 4 is repeated to close it
docs/RESTART-WINDOW.md :: retarget :: | comment out `aclfile`, restart, fix, re-dry-run |
# fresh-eyes review: moved into BUS-HEALTH.md, these name rows of a table that stayed in STREAMS.md
docs/STREAMS.md :: retarget :: The `info.changes` health row spent a while naming
docs/STREAMS.md :: retarget :: Note the *permanently* in that row.

# 2026-09-18 curation (STREAMS.md -> + NETWORK-PATHS.md, CONSUMER-REGISTRATIONS.md;
# RESTART-WINDOW.md -> + INCIDENT-2026-09-10.md). CR finding 8: both source docs
# were over the per-doc budget.
#
# AGENTS.md - the two index lines whose docs were split. NOT `index`: that warrant
# is for a line extended in place, and these had to be rewritten - each described
# content that is no longer in the doc it names. Keeping the old words to satisfy
# a checker is the stale claim this skill exists against; the words themselves
# survive in the new NETWORK-PATHS.md and INCIDENT-2026-09-10.md index lines.
AGENTS.md :: retarget :: - [docs/STREAMS.md](docs/STREAMS.md) - which streams exist
AGENTS.md :: retarget :: - [docs/RESTART-WINDOW.md](docs/RESTART-WINDOW.md) - restarting
# AGENTS.md - Layout line: the incident it named is now its own entry below it
AGENTS.md :: retarget :: the cohort restart window, its identities, the 2026-09-10 incident

# 2026-09-22 curation (BUS-HEALTH.md -> + MEMORY-PROTECTION.md,
# UNDELIVERED-CONSUMERS.md). The doc was 12,021 tokens against a 10,000 per-doc
# budget; two topics that were not "what the probe watches" left it.
#
# The DLQ bullet and the XDEL paragraph under it: that paragraph sat at column 0
# INSIDE the bullet list, which ends the list early wherever it renders. Fixing
# it indented the paragraph into its bullet and moved the terminator from the
# first line to the last. Same claims, and --claims confirms 0 atoms dropped.
docs/BUS-HEALTH.md :: tighten :: named and the entries captured; see *Who drains a DLQ* in [STREAMS.md](STREAMS.md);
docs/BUS-HEALTH.md :: tighten :: nobody did. `XTRIM` stays granted for the retention trims in the [STREAMS.md](STREAMS.md) table.
# The moved subsection's heading. `###` -> `##` is normalised; dropping the
# provenance from the heading text is not, and it is what check-seams requires
# (a `#\d{2,}` in a permanent anchor slug). The ref survives in the new doc's
# opening, and AGENTS.md and the eviction-policy finding already named this
# heading WITHOUT the ref - so the rename is what makes those two resolve.
docs/BUS-HEALTH.md :: rename :: ### `noeviction` is load-bearing beyond refusing writes (CannObserv/broker#9)
# AGENTS.md - the `noeviction` rule's pointer. The section it names moved to
# docs/MEMORY-PROTECTION.md in this same change, so the corrected line is a
# different line; the words it points AT are unchanged over there.
AGENTS.md :: retarget :: `docs/BUS-HEALTH.md`, *`noeviction` is load-bearing beyond refusing writes*;
# CR 10-12: three relative pointers inside the moved section. They travelled
# verbatim, so no-loss saw nothing, and they name no filename, so check-seams
# saw nothing either - but "above" and "here" meant BUS-HEALTH.md, where the
# checks they contrast with stayed. Retargeted at the doc by name.
docs/BUS-HEALTH.md :: retarget :: which is the healthy value. The two-tick rule above catches a consumer wedged
docs/BUS-HEALTH.md :: retarget ::    `pending` count and the group names the checks above read out of the same
docs/BUS-HEALTH.md :: retarget :: **A group whose consumer is gone was invisible to every other check here**, and
# CR 13: the preamble claimed this file expands STREAMS.md's `Producer
# durability under OOM` column. That expansion IS the section this run moved,
# so the sentence became false the moment it left.
docs/BUS-HEALTH.md :: retarget :: `Health primitive` and `Producer durability under OOM` columns this file expands.
docs/BUS-HEALTH.md :: retarget :: The probe's `backup` and `persistence` findings are [RECOVERY.md](RECOVERY.md)'s.
