# Semantic-index exclusions (CannObserv/broker#17), layered on SocratiCode's
# built-in defaults and .gitignore. Pinned by tests/deploy/test_socraticode_config.py.

# Vendored gregoryfoster/skills. skills/ and .claude/skills/ are symlinks into
# skills-vendor/, and .skills/ holds managing-skills' installed doctor.sh copy and
# curating-context's telemetry; broker authors no skill of its own, so all four go
# whole. Unexcluded they outnumber broker's own files and every sibling's
# includeLinked search would serve them as broker.
skills-vendor/
skills/
.claude/skills/
.skills/

# Worktrees: using-git-worktrees' fallback root and the harness's, both inside
# this tree.
.worktrees/
.claude/worktrees/
